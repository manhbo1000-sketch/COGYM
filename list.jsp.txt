list.jsp
<%@ page contentType="text/html;charset=UTF-8" pageEncoding="UTF-8" %>
<%@ taglib prefix="c" uri="jakarta.tags.core" %>
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Danh sách người dùng</title>
    <style>
        *{box-sizing:border-box}
        body{font-family:'Segoe UI',Arial,sans-serif;background:#f5f7fa;margin:0;padding:30px}
        .container{max-width:1000px;margin:0 auto;background:#fff;border-radius:12px;box-shadow:0 4px 16px rgba(0,0,0,.08);overflow:hidden}
        .header{background:linear-gradient(135deg,#1b2a7a,#3b5bdb);color:#fff;padding:24px 32px;display:flex;justify-content:space-between;align-items:center}
        .header h1{margin:0;font-size:22px}
        .btn{padding:10px 18px;border-radius:6px;text-decoration:none;font-weight:600;font-size:14px;display:inline-block;transition:.2s}
        .btn-add{background:#fff;color:#1b2a7a}
        .btn-add:hover{background:#f15a24;color:#fff}
        .btn-edit{background:#3b82f6;color:#fff;padding:6px 12px;font-size:13px}
        .btn-del{background:#ef4444;color:#fff;padding:6px 12px;font-size:13px}
        .btn-edit:hover{background:#2563eb}
        .btn-del:hover{background:#dc2626}
        .toolbar{padding:16px 32px;background:#f8fafc;border-bottom:1px solid #e2e8f0;display:flex;gap:12px;flex-wrap:wrap;align-items:center}
        .toolbar input,.toolbar select{padding:10px 14px;border:1px solid #cbd5e1;border-radius:6px;font-size:14px;outline:none}
        .toolbar input{width:220px}
        .toolbar input:focus,.toolbar select:focus{border-color:#1b2a7a}
        .btn-search{background:#1b2a7a;color:#fff;padding:10px 20px;border:none;border-radius:6px;cursor:pointer;font-weight:600}
        .btn-search:hover{background:#f15a24}
        .btn-reset{background:#e2e8f0;color:#334155;padding:10px 20px;border:none;border-radius:6px;cursor:pointer;font-weight:600;text-decoration:none;display:inline-block}
        .btn-reset:hover{background:#cbd5e1}
        table{width:100%;border-collapse:collapse}
        th{background:#f1f5f9;text-align:left;padding:14px 32px;color:#1b2a7a;font-size:13px;text-transform:uppercase;letter-spacing:.5px;border-bottom:1px solid #e2e8f0}
        td{padding:14px 32px;border-bottom:1px solid #f1f5f9;font-size:14px;color:#334155}
        tr:hover td{background:#f8fafc}
        .name{font-weight:600;color:#1b2a7a}
        .empty{text-align:center;padding:40px;color:#94a3b8}
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>👥 Quản lý người dùng</h1>
            <a href="users?action=new" class="btn btn-add">+ Thêm mới</a>
        </div>

        <%-- ⭐ TOOLBAR: TÌM KIẾM + SẮP XẾP ⭐ --%>
        <form class="toolbar" action="users" method="GET">
            <input type="hidden" name="action" value="list">

            <input type="text" name="country"
                   placeholder="🔍 Tìm theo quốc gia..."
                   value="${searchCountry}" />

            <select name="sortBy">
                <option value="">-- Sắp xếp theo tên --</option>
                <option value="asc"  ${sortBy == 'asc'  ? 'selected' : ''}>Tên A → Z</option>
                <option value="desc" ${sortBy == 'desc' ? 'selected' : ''}>Tên Z → A</option>
            </select>

            <button type="submit" class="btn-search">Tìm kiếm</button>
            <a href="users?action=list" class="btn-reset">Xóa bộ lọc</a>
        </form>

        <c:choose>
            <c:when test="${not empty users}">
                <table>
                    <thead>
                        <tr>
                            <th style="width:60px">ID</th>
                            <th>Tên</th>
                            <th>Email</th>
                            <th>Quốc gia</th>
                            <th style="width:160px">Hành động</th>
                        </tr>
                    </thead>
                    <tbody>
                        <c:forEach var="u" items="${users}">
                            <tr>
                                <td>${u.id}</td>
                                <td class="name"><c:out value="${u.name}" /></td>
                                <td><c:out value="${u.email}" /></td>
                                <td><c:out value="${u.country}" /></td>
                                <td>
                                    <a href="users?action=edit&id=${u.id}" class="btn btn-edit">Sửa</a>
                                    <a href="users?action=delete&id=${u.id}"
                                       class="btn btn-del"
                                       onclick="return confirm('Xóa người dùng này?')">Xóa</a>
                                </td>
                            </tr>
                        </c:forEach>
                    </tbody>
                </table>
            </c:when>
            <c:otherwise>
                <div class="empty">
                    📭 Không tìm thấy người dùng nào
                    <c:if test="${not empty searchCountry}">
                        với quốc gia "<c:out value="${searchCountry}" />"
                    </c:if>
                </div>
            </c:otherwise>
        </c:choose>
    </div>
</body>
</html>